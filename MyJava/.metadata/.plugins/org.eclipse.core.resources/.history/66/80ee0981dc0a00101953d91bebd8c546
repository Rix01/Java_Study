package work.com.workshop12.date;

import java.util.Date;

public class ConvertDate {
	public String convert(Date date, int type) {
		if(type == 1) {
			return String.format(date.getYear()+"-"+date.getMonth()+"-"+date.getDay());
		}
		return " ";
	}
}
